// Example MCP servers. Copy entries into .mcp.json (which must be strict JSON,
// no comments). Adopt MCP servers sparingly — they cost context on every turn.
// A Makefile target or a small CLI is usually a better alternative.
{
  "mcpServers": {
    // "playwright": {
    //   "command": "npx",
    //   "args": ["-y", "@playwright/mcp@latest"]
    // },
    // "filesystem-readonly": {
    //   "command": "npx",
    //   "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
    //   "env": { "MCP_READ_ONLY": "1" }
    // }
  }
}
